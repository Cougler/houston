# Houston — Claude Handoff

> Last updated: 2026-05-30
> Renamed from "mission-control-menubar" on 2026-05-12 — older references to that name in git history / past log entries point to this same project.

## What This Is
Mac menubar app that sits in the top-right of the screen and shows live context % for every running `claude` CLI session. Drops down into a 4-tab popover (Projects / Servers / Skills / Settings). Currently injects only `/start-mission` (the rest of the mission lifecycle is user-typed because of unresolved focus-targeting bugs in keystroke injection). Houston is the packaged, productized successor to Aaron's local Next.js Mission Control dashboard at `~/Apps/mission-control` — intended for paid distribution.

## Status
Houston's onboarding flow has been rewritten to match the linked Figma design — a clean white window with hidden title bar, an animated Welcome screen that auto-advances, and a top-bar chrome (Back / step dots / Skip) with a floating circular arrow as Continue. Background art (rocket+smoke on Welcome, astronaut bottom-right on Projects-folder + Terminal-apps) lives in a dedicated z-0 layer with SVG aspect ratios restored, multi-terminal selection + permissions step are wired in, and step content is vertically centered. Next: fully restart Electron (main-process changes don't hot-reload) and walk through the new flow end-to-end.

## Stack
- Electron 42 (main process: Node, renderer: Chromium)
- Next.js 16.2.6 (App Router, `output: 'export'` for static bundle into Electron)
- React 19
- Tailwind 4 (`@import "tailwindcss"` in globals.css)
- TypeScript
- `osascript` / AppleScript for terminal control (Terminal.app via tty-precise injection for legacy injectCommand; iTerm2 via tty-precise; Ghostty via System Events keystrokes — for Start mission's post-spawn `/rename` and `/start-mission` only)
- `tmux send-keys` for tmux pane injection (unwired now that mission chains are gone)
- Native Electron `dialog.showOpenDialog` for folder picker
- electron-builder 26 for DMG packaging; code-signed with the user's Apple Development cert (not notarized — fine for local install)

## Key Locations
- **App**: `~/Apps/houston/`
- **GitHub**: https://github.com/Cougler/houston (private)
- **Dev server**: `npm run dev` → Next.js on `http://localhost:3401` + Electron popover (concurrently + wait-on)
- **Packaged build**: `npm run build` → `dist/Houston-0.1.0-arm64.dmg` (~189 MB, signed)
- **electron-builder identity**: `appId: com.aaroncougle.houston`, `productName: Houston`, `mac.icon: public/icon.png` (1024×1024, auto-converted to `.icns`)
- **App icon source**: `public/icon.png` (1024×1024)
- **Tray icons**: `electron/icons/tray.png` (21×22) + `tray@2x.png` (41×44) — both regenerated from `public/tray@2x.png` master via `sips -Z`
- **User settings**: `~/Library/Application Support/Houston/settings.json` — keys: `terminals[]`, `projectsDir`, `spawnMode`, `permissionsRequestedAt`. Legacy single-`terminal` field is migrated to `terminals[]` on read and dropped.
- **Onboarding flag**: `~/Library/Application Support/Houston/onboarded.json` (existence-based)
- **Figma source**: https://www.figma.com/design/DiTvczoWOd98QMG3o9AnMF/Houston — onboarding frames `1:2965` (Welcome), `37:44` (Projects-folder), `223:60` (Terminal-apps), `37:88` (How-it-works), `260:211` (Projects-tab)
- **Onboarding assets**: `public/onboarding/` — `rocket.svg`, `smoke.svg`, `astronaut.svg`, `house.svg` (all stripped of `preserveAspectRatio="none"` so aspect ratios are preserved), terminal icons `icon-ghostty.png` / `icon-iterm.png` / `icon-terminal.png`, and `screenshot-projects.png` / `screenshot-detail.png`
- **Globally updated skills**:
  - `~/.claude/skills/start-mission/SKILL.md` — step 2 offers `git init` + `gh repo create --private --push` for projects without `.git` or remote (pre-flight scan for secrets); step 3 fast-forward pulls when behind, silent otherwise, stops only on divergence.
  - `~/.claude/CLAUDE.md` `End Mission` — full wrap-up flow: status note → write project `CLAUDE.md` handoff → update `.mc.json` notes → kill dev server → git commit/push (drafts message from diff, asks once, commits with `Co-Authored-By` trailer). Mission Control coupling removed (no more `log-session.py`); `/log-mission` skill deleted (folded in here).
  - `~/.claude/skills/end-mission/SKILL.md` — quick-exit slash command: kills dev servers and exits Claude with no wrap-up. Separate from the "end mission" phrase trigger above.
- **Authoritative session data on disk**: `~/.claude/sessions/<pid>.json` per running claude (sessionId, cwd, startedAt, version, kind, entrypoint)

## Architecture Notes
- `electron/main.js` — entry. Creates the menubar Tray, a frameless BrowserWindow popover, IPC handlers, and calls `app.dock.hide()` for menubar-only mode (`LSUIElement: true` in build config for the packaged app). Tray right-click menu: Reload / Help → Show onboarding / Quit Houston. **In production, loads the renderer via a custom `app://` scheme** (`win.loadURL("app://-/index.html")`, `onboardingWin.loadURL("app://-/onboarding/index.html")`). The scheme is registered as privileged (`standard + secure + supportFetchAPI`) at module load time. Inside `app.whenReady`, `protocol.handle("app", ...)` maps `app://-/<path>` → `file://<PROD_OUT_DIR>/<path>` via `net.fetch(pathToFileURL(...).toString())`. Required because Next's static export emits root-absolute paths (`/_next/...`, `/houstonlogo.png`) that don't resolve under `file://`.
- **Onboarding window chrome** (`createOnboardingWindow`): `titleBarStyle: "hiddenInset"` with `trafficLightPosition: { x: 14, y: 14 }` so the native traffic lights float over a borderless white canvas — there is no visible title bar. `page-title-updated` is preventDefault'd so the page's `<title>` (still `Houston` from root metadata) never propagates to the OS window title.
- `electron/preload.js` — `contextBridge.exposeInMainWorld('mc', ...)` exposes: `getDashboard`, `getActiveSessions`, `getProjectDetail`, `getSkillDetail`, `getActiveServers`, `killServer`, `completeOnboarding`, `openPath`, `openExternal`, `hide`, `clearServerCache`, `startDevServer`, `getSettings`, `setSettings`, `pickDirectory`, `startMission(projectPath, terminal?, mode?)`, and the new `checkPermissions(terminals[])`, `requestPermissions(terminals[])`, `openPermissionsSettings(pane)`.
- `electron/settings.js` — reads/writes `<userData>/settings.json`. Validates `terminals[] ⊆ ["Ghostty", "Terminal", "iTerm2"]`, `spawnMode ∈ ["tab", "window"]`. Expands `~` in `projectsDir`. Defaults: `terminals: ["Ghostty"]` / `~/Apps` / window. Legacy `terminal: string` is migrated to `terminals[]` on read and dropped. `terminals[0]` is the primary used for spawn-mode logic; Terminal-as-primary forces window mode (Sequoia tab limitation).
- `electron/main.js` permissions IPC (new): `permissions:check` returns `{ accessibility, automation: Record<terminal, bool>, permissionsRequestedAt }` — Accessibility via `systemPreferences.isTrustedAccessibilityClient(false)`, Automation via a no-op `osascript "tell application X to get name"` probe per selected terminal. `permissions:request` triggers the native prompts on first call (sets `permissionsRequestedAt`), deep-links to System Settings on subsequent calls if anything is still missing.
- `electron/data.js` — `getDashboardData(projectsDir)` and `getProjects(projectsDir)` take the projects directory as a parameter; `getDashboardData` returns `projectsDir` in its payload so the renderer can show it in the empty state. Project icons are auto-detected and embedded as data URLs.
- `electron/dev-servers.js` — `getActiveServers(projectsDir)` lists every TCP listener in ports 3000–9999 via `lsof`, resolves cwd via `lsof -d cwd`, and labels the row with the project basename when cwd is under `projectsDir`.
- `electron/process-detect.js` — load-bearing detection module. Uses `ps -axo pid,ppid,lstart,command` for the snapshot. For each claude PID it reads `~/.claude/sessions/<pid>.json` for the cwd (definitive when present, else `lsof -a -p <pid> -d cwd -Fn` fallback). To pick the JSONL it prefers the **latest-mtime** JSONL in `~/.claude/projects/<encoded-cwd>/` whose mtime ≥ the claude PID's `startMs` — because Claude does NOT update the session file's `sessionId` after `/clear`, the file points at a stale JSONL post-clear, so trusting it gives stale token counts. Birth-time-to-process-start matching (±30s) is the last-resort fallback for older claude versions with no session file.
- **Context size math**: `contextSize = input_tokens + cache_read_input_tokens + cache_creation_input_tokens + output_tokens` from the latest assistant message.
- `electron/terminal/index.js` — adapter dispatch + `startMission` only. Snapshots `~/.claude/sessions/*.json` before spawn → polls up to 30s for a new sessions file whose `cwd` matches → 1500ms settle → keystrokes `/rename <sanitized-projectName>` + Enter → 800ms settle → keystrokes `/start-mission` + Enter.
- `electron/terminal/<adapter>.js`:
  - `terminal-app.js spawnSession(path, cmd, "window"|"tab")` — Sequoia limitation: tab AppleScript dead-ends; workaround is "Prefer tabs when opening documents" → Always.
  - `iterm2.js spawnSession(path, cmd, "window"|"tab")` — both modes work cleanly.
  - `ghostty.js spawnSession(path, cmd, "window"|"tab")` — tab mode uses `application "Ghostty" is running` check + Cmd+T via System Events. Falls back to new window if Ghostty isn't already running.
- `app/page.tsx` — single client component. Polls `getActiveSessions()` every 2s, `getDashboard()` every 30s, `getActiveServers()` every 4s. Header has a sync icon (`SyncIcon`, now using Lucide's `refresh-cw` paths at viewBox 24 / strokeWidth 2) that re-fires all three polls in parallel via `handleSync` and spins for 400ms minimum. Four tabs: Projects / Servers / Skills / Settings. **Settings view** uses `terminals[]` multi-select (checkmark badges, refuses zero-selected, treats `terminals[0]` as the spawn primary).
- `app/onboarding/layout.tsx` — overrides root metadata with `title: ""` so the document title stays blank on the onboarding route.
- `app/onboarding/page.tsx` — full Figma rewrite. 10 steps in order: `welcome` → `projectsDir` → `terminal` → `permissions` → `howItWorks` → `tabProjects` → `tabSkills` → `tabServers` → `tabSettings` → `done`. White hardcoded color palette (`C` token object: bg `#ffffff`, body `#737373`, small `#424242`, accent `#5900ff`, surfaces `#f0f0f0`/`#f5f5f7`/`#f6f6f6`). Top chrome (rendered everywhere except `welcome`): Back at `left:80, top:14`, step dots centered horizontally at `top:20`, Skip at `right:20, top:14`, and a 44×44 black circle Continue arrow at `right:24, top:50%`. All chrome is z-40 with `WebkitAppRegion: no-drag`; there's a 40px drag handle across the top at z-20. `BackgroundLayer` (z-0, separate from content) holds the astronaut on `projectsDir`+`terminal` only (`right:-170, bottom:-80, height:560, opacity:0.6` — multiplied with the SVG-baked 0.05 so it sits as a very faint backdrop). Step content uses `StepShell` (vertically centered, `marginLeft:80, width:497`) or `TabSectionShell` (vertically centered, `marginLeft:80, marginRight:80`). `WelcomeStep` owns its own rocket+smoke art and a phased animation (200ms title → 1100ms subtitle → 1800ms art → 4500ms auto-advance); footer + Continue + Skip + step dots are all hidden on `welcome`.
- `next.config.ts` — `output: 'export'`, `trailingSlash: true`, `images.unoptimized: true`. Electron loads via `app://-/index.html` in production; `http://localhost:3401` in dev.
- `package.json` — `name: "houston"`, `build.appId: "com.aaroncougle.houston"`, `build.productName: "Houston"`, `build.mac.icon: "public/icon.png"`, `build.mac.extendInfo.LSUIElement: true` (menubar-only).

## Recent Changes (this session)
- **Onboarding rewritten end-to-end to match the Figma.** New `app/onboarding/page.tsx`: 10 steps in Figma order (Welcome → Projects folder → Terminal apps → **Permissions** [my addition] → How it works → Projects tab → **Skills tab** + **Servers tab** + **Settings tab** [my designs] → You're all set). Hardcoded white palette so the view is theme-independent.
- **Welcome screen is animated and auto-advances.** Phased fade-in: title up at 200ms, subtitle up at 1100ms, rocket+smoke fade-in (no motion) at 1800ms, auto-advance to step 2 at 4500ms. Footer/Continue/Skip/step dots all hidden on Welcome. Rocket uses the Figma's natural `height:108`, smoke uses `width:365`, smoke is anchored `bottom:0`, rocket at `bottom:168` — touching the bottom of the window with no nav reserved.
- **Chrome restructured.** Removed bottom footer. Back top-left (`left:80, top:14`), step dots centered at top (`top:20`), Skip top-right (`right:20, top:14`). Continue is now a 44×44 black circular button with a chevron-right arrow at right vertical center. Back is disabled on steps 0+1 (going back to Welcome would re-trigger its auto-advance loop).
- **Background art is a true z-0 background layer.** `BackgroundLayer` component, separate from step content, at `inset-0 z-0 overflow-hidden pointer-events-none`. Astronaut anchored bottom-right (`right:-170, bottom:-80, height:560, opacity:0.6`) so the figure inside its SVG (which ends at ~87% of viewBox height) actually touches the window bottom; ~33% is clipped off the right edge.
- **Window chrome (Electron).** `createOnboardingWindow` switched to `titleBarStyle: "hiddenInset"`, `trafficLightPosition: { x:14, y:14 }`, `title: ""`. Added `onboardingWin.on("page-title-updated", e => e.preventDefault())` so Next's root `<title>Houston</title>` doesn't bubble up to the OS window. New `app/onboarding/layout.tsx` overrides root metadata with `title: ""` for the route. **Note**: main-process changes don't hot-reload — must restart `npm run dev` to see them.
- **SVG distortion fix.** All four onboarding SVGs (`rocket`, `smoke`, `astronaut`, `house`) had `preserveAspectRatio="none"` + `width="100%" height="100%"` baked into the file. Stripped both so the browser respects the viewBox and aspect ratios are preserved naturally when only one dimension is set.
- **SVGs that were `.png` are now `.svg`.** `astronaut`, `house`, `rocket`, `smoke` were SVG bytes with `.png` extension — renamed for proper MIME handling.
- **Multi-terminal selection + permissions step.** Onboarding `terminal` step is multi-select with a checkmark badge selected state. Settings migrated from single `terminal: string` to `terminals: string[]` (back-compat migration in `electron/settings.js`). New Permissions step (between Terminal and How-it-works) probes Accessibility via `systemPreferences.isTrustedAccessibilityClient`, Automation via per-terminal `osascript "tell application X to get name"` no-op probe. CTA triggers native prompts on first ask, deep-links to System Settings after. Polls every 1.5s so externally-granted permissions light up automatically.
- **Step content vertically centered.** `StepShell` and `TabSectionShell` both switched to `flex items-center` so title + body + interactive block sit as a single vertically-centered block in the 600px window.
- **How-it-works rows widened.** Row container bumped from `width:497` → `width:620` (extends past StepShell's right edge). Command-name column inside `CommandRow` bumped from `width:95` → `width:120` with `whiteSpace:"nowrap"` so `/start-mission` stays on a single line.
- **Skills/Servers/Settings tab pages are my designs.** No screenshots provided, so I built stylized mock UI rows in the same row pattern as How-it-works. If you want real screenshots, drop them in `public/onboarding/screenshot-skills.png` / `screenshot-servers.png` / `screenshot-settings.png` and I'll swap them in next session.

## What's Next
- **Fully restart `npm run dev`** to pick up the `electron/main.js` changes (`titleBarStyle: "hiddenInset"`, the `page-title-updated` block, the new permissions IPC handlers). Main-process changes don't hot-reload.
- **Walk through the full onboarding flow** end-to-end in dev. Verify: Welcome animation timing feels right, auto-advance lands cleanly on Projects-folder, astronaut sits where it should on Projects-folder + Terminal, permissions prompts fire on first ask, multi-select terminal toggles persist, Skip/Back/Continue all work, step dots reflect position.
- **Test the permissions flow on a clean machine** (or after revoking grants in System Settings) — confirms both the native first-prompt and the deep-link fallback work.
- **Drop real screenshots for Skills/Servers/Settings** tab pages if the mock rows feel underwhelming compared to the Projects tab.
- **Carried over**: rebuild + install the DMG and verify the new onboarding in the packaged build (icon sharpness on Retina, settings persistence across relaunches, app icon in Finder + DMG window + About dialog).
- **Carried over**: single-instance lock (`app.requestSingleInstanceLock()` if dock-launcher clicks while Houston is running spawn duplicate instances), Terminal.app tab-mode workaround documented in Settings sub-label, nodemon-style watcher so Electron auto-restarts on `electron/*.js` edits, decision on stale `missionlog.md`, Swift helper for Ghostty AX targeting.

## Known Issues / Gotchas
- **Main-process changes don't hot-reload.** Edits to anything under `electron/*.js` require Ctrl+C + rerun `npm run dev`. Recurring source of "why isn't this working" confusion.
- **Backing up to Welcome would auto-loop.** Welcome auto-advances 4.5s after mount, so if Back were enabled on step 1 the user would be ping-pong'd back to step 2 every time. Back is disabled until step 2.
- **The Skills/Servers/Settings tab pages are mocks**, not real screenshots. Designed to be modifiable — drop in real PNGs at `public/onboarding/screenshot-*.png` to upgrade.
- **Permissions step probes block** until the user dismisses the macOS prompt (Automation prompts on first ask use a real Apple Event). They run in `Promise.all` so multiple prompts queue rather than serializing.
- **Terminal.app "Tab" spawn mode opens a new window** under macOS Sequoia. AppleScript dead-ends: `do script "..." in front window` runs in the active tab; `make new tab` is unsupported; synthesized Cmd+T via key code 17 silently no-ops; "New Tab" menu item creates a new window. Workaround: System Settings → Desktop & Dock → "Prefer tabs when opening documents" → Always.
- **Ghostty "Tab" mode is focus-sensitive**. `tell application "Ghostty" to activate` brings Ghostty's most-recently-focused window forward; Cmd+T creates the new tab there.
- **Residual focus race for the post-spawn `/rename` and `/start-mission` keystrokes**.
- **Settings tab shows "Loading…" if Electron hasn't picked up the new IPC handlers** — happens whenever `electron/*.js` changes hot-reload skips. Manual fix: Ctrl+C and rerun `npm run dev`.
- **30s timeout on session-file readiness** for Start mission.
- **Port 3401 is hardcoded** for dev. Change in both `package.json` scripts and `electron/main.js` `DEV_URL`.
- **First-run permissions for the packaged Houston.app**: needs Accessibility (System Events keystrokes for `/rename` + `/start-mission`) + Automation (controlling Terminal/iTerm/Ghostty for spawnSession) grants. These are per-bundle, so the packaged Houston.app is a different identity than the dev Electron.app — the user will see fresh permission prompts on first packaged-app launch.
- **electron-builder warnings**: `description` and `author` fields missing in `package.json`; duplicate `react-dom@19.2.4` in the dep tree; macOS notarization is skipped (`notarize` options not generated). None affect local install/testing; matter for outside-the-machine distribution.
- **Process detection is read-only** — the app never modifies `~/.claude/projects/` JSONLs or `~/.claude/sessions/*.json`.
- **Session JSON files are not always present.** Only recent claude versions emit `~/.claude/sessions/<pid>.json`. Code falls back to birth-time correlation when missing.
- **No projection of in-flight tokens.** If the user has just typed a message but Claude hasn't responded yet, that message isn't in the JSONL usage block.
